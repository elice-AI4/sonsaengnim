import React from "react";
import { curSelectedButtonProps } from "..";

interface WordImageListPorops {
  curSelectedButton: curSelectedButtonProps;
}

const WordImageList = ({ curSelectedButton }: WordImageListPorops) => {
  return <h1></h1>;
};

export default WordImageList;
