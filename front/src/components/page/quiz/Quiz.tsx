import React from "react";
import { QuizContainer } from "./Quiz.style";

const Quiz = () => {
  return (
    <QuizContainer>
      <button>퀴즈 맞추러 가기</button>
    </QuizContainer>
  );
};

export default Quiz;
